// TODO: implement in Phase 1 task 4 / Phase 2 (see /data-mapping and /graphql skills)
// Will call fetchOrFail from src/lib/hygraphClient.ts, transform PlayerRD into a
// PlayerVM (to be defined in ./types once this is written). On failure, render nothing
// per /graphql skill — this model gets no fallback.ts.

export async function getPlayerVMs(): Promise<unknown> {
  throw new Error("Player mapper not implemented yet");
}
