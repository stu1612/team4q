// TODO: implement in Phase 1 task 4 / Phase 2 (see /data-mapping and /graphql skills)
// Will call fetchOrFail from src/lib/hygraphClient.ts, transform TrainingRD into a
// TrainingVM (to be defined in ./types once this is written). On failure, render the
// full message block per /graphql skill — this model gets no fallback.ts.

export async function getTrainingVMs(): Promise<unknown> {
  throw new Error("Training mapper not implemented yet");
}
